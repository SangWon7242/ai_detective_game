# AI 탐정단 웹 게임 설계

## 목표

초등학교 4~5학년 AI 엔트리 수업 도입부에서 사용할 수 있는 아이스브레이킹 웹 게임을 만든다. 학생은 지도 속 장소를 클릭하고, 생활 속 기능이 AI인지 아닌지 추리한다.

## 파일 구성

- `index.html`: 시작 화면, 지도 화면, 문제 모달, 결과 화면의 기본 구조를 제공한다.
- `style.css`: 파스텔톤 지도, 카드, 버튼, 반응형 배치, 정답/오답 애니메이션을 담당한다.
- `script.js`: 문제 데이터, 화면 전환, 정답 판정, 점수 계산, 진행률, 결과 등급, 다시 시작 로직을 담당한다.

## 화면 흐름

1. 시작 화면에서 게임 제목, 설명, 시작 버튼을 보여준다.
2. 시작 버튼을 누르면 지도 화면으로 이동한다.
3. 지도 화면에는 `questions` 배열을 기준으로 장소 버튼을 렌더링한다.
4. 장소를 클릭하면 문제 모달을 연다.
5. 학생이 `AI예요` 또는 `AI가 아니에요`를 선택하면 정답 여부와 설명을 같은 모달에 보여준다.
6. 조사한 장소는 흐리게 표시하고 체크 표시를 붙인다.
7. 모든 장소를 조사하면 결과 화면으로 이동한다.
8. 다시 시작 버튼으로 점수와 조사 상태를 초기화한다.

## 학습 메시지

게임 화면과 결과 화면에 다음 기준을 노출한다.

- AI에 가까운 경우: 사진, 목소리, 글을 알아봄 / 행동을 보고 추천함 / 상황을 보고 판단함 / 사람처럼 대답하거나 분류함
- AI가 아닌 경우: 정해진 버튼대로 움직임 / 단순 계산만 함 / 정해진 시간에 작동함 / 새로운 판단 없이 반복함

## 접근성 및 수업 사용성

- 버튼은 키보드로 이동하고 실행할 수 있게 `<button>` 요소를 사용한다.
- 모달은 `role="dialog"`와 `aria-modal="true"`를 사용한다.
- 프로젝터와 태블릿에서 잘 보이도록 큰 글씨, 큰 터치 영역, 높은 대비를 적용한다.
- 외부 이미지와 라이브러리는 사용하지 않고 이모지와 CSS만 사용한다.
